// UNVALIDATED SCAFFOLD (see src/lib.rs). Standard Tauri 2.0 build script: it wires the
// generated context, capabilities, and platform metadata at compile time.
fn main() {
    tauri_build::build();
}
